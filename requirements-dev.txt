# Development & test tooling. Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
# These are intentionally excluded from the production image.

-r requirements.txt

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-mock==3.14.0
factory-boy==3.3.0

# Lint / format (configured in pyproject.toml)
black==24.4.2
isort==5.13.2
ruff==0.15.20
